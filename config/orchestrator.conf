# orchestrator.conf
# All settings are optional — built-in defaults are used for any missing key.
# Lines starting with '#' are comments.

# ── HTTP server ───────────────────────────────────
listen_port         = 8080

# ── Paths ─────────────────────────────────────────
# Linux defaults (uncomment and adjust for Windows):
# work_dir           = C:\orch\jobs
# db_path            = C:\orch\orchestrator.db
# provisioning_json  = C:\orch\provisioning.json

work_dir            = /var/orch/jobs
db_path             = /var/orch/orchestrator.db
provisioning_json   = /etc/orch/provisioning.json

# ── Logging ───────────────────────────────────────
# Levels: debug | info | warn | error
log_level           = info

# ── Scheduler ─────────────────────────────────────
# How often the scheduler checks for runnable jobs (milliseconds)
scheduler_poll_ms   = 500

# ── Cleanup ───────────────────────────────────────
# Seconds after a job reaches a terminal state before its
# work directory (input/ + output/) is deleted.
# Set to 0 to disable automatic cleanup.
cleanup_ttl_seconds = 3600
